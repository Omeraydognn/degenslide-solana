# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor / OS
.DS_Store
Thumbs.db
*.log
.vercel

# SQLite transient journals (the checkpointed .db files are the real data)
*.db-shm
*.db-wal
# stale local DB backups — never commit these
*.bak
